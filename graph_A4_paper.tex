\documentclass[a4paper]{article}
\usepackage[top=0cm, bottom=0cm, left=0cm, right=0cm]{geometry}
\usepackage{tikz}
\usepackage{eso-pic}

% grid setup - 5mm
\newcommand{\gridSize}{0.5cm}      % 5mm
\newcommand{\gridColor}{blue!20}   % Azul bem claro

\AddToShipoutPicture{%
	\begin{tikzpicture}[remember picture, overlay]
		% Main Grid de 5mm
		\draw[step=\gridSize, \gridColor, very thin] 
		(current page.south west) grid (current page.north east);
		
		% Bold lines every 2 cm (4 squares = 20 mm)
		\draw[step=4*\gridSize, \gridColor, thin] 
		(current page.south west) grid (current page.north east);
		
		% page margin
		\draw[black!70, thick] 
		(current page.south west) rectangle (current page.north east);
	\end{tikzpicture}%
}

\begin{document}
	\pagestyle{empty}
	
	\vspace*{8cm}
	\begin{center}
		{\Huge Quadriculado 5mm}\\[0.5cm]
		{\Large Sem margens - página inteira}
	\end{center}
	
\end{document}
